import '@testing-library/jest-dom'

// Add any global test setup here

// Ensure test environment is set
process.env.NODE_ENV = 'test';

// Optionally set LOG_LEVEL to silent for extra safety
process.env.LOG_LEVEL = 'silent';
